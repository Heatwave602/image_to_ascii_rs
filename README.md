# image_to_ascii

An image to ascii converter written in rust using the "image" crate.

## TODO
1. check whether the image needs resizing or not(?)
2. add support to colored images
3. add edges to the image